# Sample Report Fixtures

Small tracked report fixtures used to smoke-test and demonstrate the reporting
dashboard on a clean checkout. Full generated run artifacts belong in ignored
`reports/` directories. Treat the fixture metrics as UI smoke data; final
experiment metrics are regenerated with `scripts/python/run_experiments.py` and
reported in `docs/report.md`.
